const WebSocket = require('ws');

const WebSocketServer = WebSocket.Server;


// 实例化:
const wss = new WebSocketServer({
    port: 3000
});


class webSocket {
	constructor(){
	}
	Connect(params, callback){
		
		
		
	}
	
}
exports.service = webSocket;
